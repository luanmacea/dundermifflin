import styled from "styled-components";

const HeaderContainer = styled.header`
  background-color: #0047da;
`;

function Header() {
  return <HeaderContainer>Dunder Mifflin</HeaderContainer>;
}

export default Header;
